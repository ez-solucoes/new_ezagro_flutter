import 'package:flutter/material.dart';

class DynamicInfoCard extends StatelessWidget {
  const DynamicInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
